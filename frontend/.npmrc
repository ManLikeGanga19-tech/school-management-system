# next-intl 3.x declares peer next@^10-^15 but we run Next.js 16.
# All packages resolve correctly at runtime; --legacy-peer-deps bypasses
# the strict peer resolution check without altering the install graph.
legacy-peer-deps=true
